spring.flyway.locations=classpath:/db/migration,classpath:/dev/db/migration
spring.h2.console.enabled=true

logging.level.lab=DEBUG
logging.level.lab.spring.http.LoggingHttpRequestInterceptor=TRACE

# Assume a fixed identity when no gateway is in front of the service.
mops.identity.development.enabled=true
