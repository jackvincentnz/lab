query GetChat($id: ID!) {
  chat(id: $id) {
    id
    messages {
      id
      type
      status
      content
      toolCalls {
        id
        name
        arguments
        status
      }
      createdAt
      updatedAt
    }
    createdAt
    updatedAt
  }
}
